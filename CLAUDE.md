# ControlLeads

Lead management system (CRM/funnel) for **international student recruitment**. Single institution (single-tenant). **Entire product in English** (UI, enums, API, code); project docs in Portuguese.

## Monorepo layout

```
backend/   Spring Boot (Java) + PostgreSQL — single source of business logic
web/       Angular (signals) + ECharts — animated dashboards        [not scaffolded yet]
app/       Flutter (Android + iOS) — same APIs as web               [not scaffolded yet]
shared/
  api-contract/    openapi.yaml exported from springdoc → generates TS + Dart clients
  design-tokens/   tokens.json (Style Dictionary) → CSS vars (web) + ThemeData (app)
.spec/     SDD discovery & specs — READ THIS FIRST
```

## Where the truth lives

- **`.spec/discovery/`** — approved product discovery: `brief.md`, one `module_*.md` per module (RFs/RNs), `entities.md` (data model), `roadmap.md` (execution order, 15 suggested SPECs).
- **`.spec/memory/`** — structural decisions (overview, phases, stack).
- **`backend/src/main/resources/db/migration/V1__initial_schema.sql`** — initial schema, already written from entities.md. Treat as the data-model reference.
- **`shared/design-tokens/tokens.json`** — visual source of truth incl. per-status colors.

## Core domain rules (do not violate)

1. **Funnel**: `LEAD → HOT_LEAD → APPLICATION → STUDENT` + `STALLED` (paused, records `stalled_from_status` + `stall_reason`). STALLED reactivates back to the stage it left. STUDENT is terminal (admin-only correction).
2. **Every status change writes an immutable `lead_status_events` row** — ALL funnel/drop-off/conversion reports derive from events, never from current status.
3. **Ownership visibility**: `MARKETING_TEAM` users see/edit only leads where `assigned_to = them`; `ADMINISTRATOR` sees all. Enforced in the backend on every endpoint, never only in UI.
4. **Aggregations happen only in the backend** — web and app must show identical numbers.
5. Catalogs (`courses`, `channels`, `stall_reasons`) and `users` are **deactivated, never deleted** (history references them).
6. Contact activities (`CALL`, `EMAIL`, `WHATSAPP`, `MEETING`) update `leads.last_contacted_at` — the SLA clock. SLA applies to HOT_LEAD only; max 1 alert per lead per cycle.

## Architecture decisions (settled — don't relitigate)

- UUIDs generated by the application; optimistic locking (`version`) on `leads`.
- Auth: JWT access + rotating refresh; roles `ADMINISTRATOR` | `MARKETING_TEAM`.
- Contract-first between clients: springdoc exports OpenAPI → generated typed clients (TS for Angular, Dart for Flutter). Never hand-write API models in clients.
- Errors: RFC 7807 ProblemDetails end-to-end.
- Charts: ECharts (web), fl_chart (app, map spike pending — fallback: bar ranking by country).
- Duplicate lead (same email/phone): **warn, don't block**.

## Current state / next steps

**Fase 0 DONE** (all verified locally): discovery in `.spec/discovery/`; Spring Boot 4.1/Java 25 backend (Flyway V1 applied, tests green via Testcontainers); Angular 22 web (build+tests green, consumes `/api/ping` through proxy); Flutter app (analyze+test green, tokens-based theme); tokens build (`node shared/design-tokens/build.mjs`); contract pipeline validated end to end (`./scripts/sync-api-contract.sh` → openapi.yaml → TS + Dart clients); docker-compose; GitHub Actions CI.

**Environment quirks (this machine)**: Docker via Colima — Testcontainers socket override already set in `backend/pom.xml`. Host ports 5432/5433 and 8080–8083 are taken by other projects → Postgres runs on **5434**, backend on **8090**. Don't "fix" these back.

**Fase 1a DONE** (all verified): JWT HS256 + rotating refresh (hashed at rest, V2 migration), role enforcement (@PreAuthorize + resource server), admin user management, /api/me; seeded admin `admin@controlleads.local`/`admin123` (override APP_SEED_ADMIN_*). Web: AuthService signals + interceptor + guards, login/dashboard/team pages. App: AuthService ChangeNotifier, login/home. 6/6 backend e2e tests green.
Boot 4 gotchas learned: TestRestTemplate package moved (use MockMvc — `org.springframework.boot.webmvc.test.autoconfigure.AutoConfigureMockMvc`); no com.fasterxml ObjectMapper bean (Jackson 3 default) — instantiate locally in tests; AccessDeniedException must be handled explicitly in the advice or @PreAuthorize denials become 500; Hibernate validate rejects CHAR columns for String fields (use VARCHAR).

**Fase 1b** — backend DONE and green (13/13 tests: catalogs CRUD admin-gated; lead create/update/list-with-filters/detail; transition validation writing immutable events; STALLED with required reason + reactivate-to-origin; ownership visibility returns 404 not 403; duplicates warn endpoint). Web + app lead UIs written (list/filters, form with live duplicate warning, detail with unified timeline + transitions, Kanban CDK board with stall-reason prompt, catalogs admin page). More Boot gotchas: Spring Data repos must be TOP-LEVEL interfaces (nested ones are not scanned); Postgres CHAR columns break Hibernate validate (always VARCHAR).

**Fases 1c + 1d backend DONE and green** (23/23 backend tests; new module packages `activities/` and `analytics/`). Activities: entity over the existing V1 `activities` table, ownership-gated CRUD + `/api/my/follow-ups`, contact kinds update `leads.last_contacted_at`. Analytics: `AnalyticsService` uses `NamedParameterJdbcTemplate`, ALL figures derive from `lead_status_events` (funnel = distinct leads that ever reached each stage; avgDaysToConvert dedupes to first STUDENT event; drop-off from STALLED events; scoped by owner for members, global+leaderboard for admin). OpenAPI regenerated (35 operations). One more Boot/JDBC gotcha: `NamedParameterJdbcTemplate.query(sql, params, lambda)` — an EXPRESSION lambda is ambiguous between RowCallbackHandler and ResultSetExtractor; use a BLOCK lambda `rs -> { ... }`. And in MockMvc tests, name helper methods so they don't shadow the static-imported `get`/`post` builders.

The original contract (kept for reference; now implemented) lives in `web/src/app/core/api/activities.service.ts` and `analytics.service.ts`:

- `GET /api/leads/{id}/activities` → `[{id, leadId, type(NOTE|CALL|EMAIL|WHATSAPP|MEETING|FOLLOW_UP), content, dueAt?, completedAt?, createdBy, createdByName, createdAt}]`
- `POST /api/leads/{id}/activities` `{type, content, dueAt?}` — contact types (CALL/EMAIL/WHATSAPP/MEETING) update `leads.last_contacted_at`
- `POST /api/activities/{id}/complete`
- `GET /api/my/follow-ups` → open FOLLOW_UPs of caller + `leadName`, ordered by dueAt
- `GET /api/analytics/summary?from&to` → `{totalLeads, newLeads, conversionRate(0..1), avgDaysToConvert|null, slaBreaches}`
- `GET /api/analytics/funnel` → `[{status, count}]` (current distribution incl. STALLED)
- `GET /api/analytics/drop-off` → `[{stage, reasonId, reasonName, count}]` from STALLED events
- `GET /api/analytics/timeseries?interval=week|month` → `[{period, created, converted}]`
- `GET /api/analytics/by-channel` and `/by-course` → `[{id, name, total, students}]`
- `GET /api/analytics/by-country` → `[{countryCode, total, students}]`
- `GET /api/analytics/leaderboard` (admin 403 otherwise) → `[{userId, name, totalLeads, students}]`
- All analytics derive from `lead_status_events` (never current status), scoped to caller's leads for MARKETING_TEAM, global for admin.

**Pending (user will run / next session)**: `web: npm install && npm run build && npm test -- --watch=false` (new dep: echarts ^6 — added to package.json, NOT installed yet) · `app: flutter analyze && flutter test` · fix fallout · commit · regenerate openapi.yaml. Web dashboard uses a hand-rolled ECharts wrapper (`web/src/app/shared/chart.ts`) — no ngx-echarts on purpose.

**Still uncommitted / pending validation** (bash classifier was flaky across sessions; user validates web/app builds themselves): commit Fases 1b+1c+1d together; run `web: npm install && npm run build && npm test` (new dep echarts ^6) and `app: flutter analyze && flutter test`.

**Fase 1e — Notifications & SLA: backend DONE and green (30/30 tests).** New package `notifications/`: `Notification` entity over the existing V1 table (payload jsonb via `@JdbcTypeCode(SqlTypes.JSON)`), `NotificationService` with `runSlaSweep()`/`runFollowUpSweep()` + anti-spam dedup (`existsByUserIdAndLeadIdAndTypeAndCreatedAtAfter`, SLA-cycle window for breaches, 24h for follow-ups), owner-addressed, in-app center endpoints (`/api/notifications`, `/unread-count`, `/{id}/read`, `/read-all`). `NotificationScheduler` (@Scheduled, gated by `app.scheduling.enabled` — tests set false in `src/test/resources/application.properties`; app class has `@EnableScheduling`). Push/email are a pluggable seam: `NotificationDispatcher` interface + `LoggingNotificationDispatcher` default via `NotificationConfig` `@ConditionalOnMissingBean` — wire real FCM/APNs + SMTP by providing your own bean. `hot_lead_max_hours` now read through shared `settings/AppSettingsService` (Analytics refactored to use it). Front: web notification bell + badge + panel in the Topbar; Flutter notifications screen + badge in the leads app bar. OpenAPI = 39 operations.

**The MVP (Fase 1a–1e) is feature-complete on the backend.**

**Column B (MVP hardening) — in progress.** Done + backend-green (34/34):
- **Password reset (RF-04)**: `/api/auth/forgot-password` + `/api/auth/reset-password`; single-use SHA-256-hashed tokens (migration V3, `password_reset_tokens`), `PasswordResetMailer` seam (`LoggingPasswordResetMailer` default via `AuthMailConfig` @ConditionalOnMissingBean — wire real SMTP here; link base = `app.web.password-reset-url`), reset revokes all refresh tokens, no email enumeration. Web: `/forgot-password` + `/reset-password?token=` pages, "Forgot password?" on login.
- **Silent refresh**: web `authInterceptor` retries once via `AuthService.refresh()` (single-flight `shareReplay`) on a 401; Flutter `ApiClient._send` retries with a `retry` flag; `AuthService.refresh()` single-flight via a shared future. Rotating refresh tokens are one-shot, hence the single-flight.

Column B "green block" (deliverable without external credentials) — DONE + backend-green (37/37):
- **CSV export**: `GET /api/leads/export.csv` reuses `LeadController.buildSpec` (same ownership scope + filters as the list). Web: "Export CSV" button (blob download through the auth interceptor).
- **Dashboard period filter**: analytics endpoints (funnel, drop-off, by-channel, by-course, by-country) take optional `from`/`to`; `AnalyticsService.cohort()` applies a `lead.created_at` cohort filter (UTC half-open) on top of the ownership scope; still event-derived. Web: date inputs + Apply/Clear on the dashboard, plus a "Print / PDF" button with a `@media print` stylesheet (the visual-report PDF).
- **Mass reassignment (RN-02)**: `POST /api/users/{id}/reassign-leads {toUserId}` (admin only) via `LeadService.reassignAll` — moves all non-deleted leads, one `LeadAssignmentEvent` each; `/api/users` now returns `leadCount`. Web Team page: Leads column + inline "Reassign" flow.

Remaining Column B (needs the user's credentials/decision): real push (FCM/APNs) + email sender beans behind the existing seams (`NotificationDispatcher`, `PasswordResetMailer`); Flutter secure token storage; and the churn item — swap hand-written client services for generated OpenAPI clients. Then Column C (Fase 2 public capture; Futuro: WhatsApp/documents/nurture/TEAM_LEAD) and Column D (deploy/Dockerfile/CI-prod, front + E2E tests, observability).

Open decisions (small, decide in specs): activity edit window; email digest vs immediate; lead auto-assignment for Fase 2 public capture.
